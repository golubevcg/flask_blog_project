version: '3'

services:
  pg:
    image: postgres
    environment:
      POSTGRES_DB: flask_blog
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD:
    ports:
      - "5432:5432"
    volumes:
      - db-data:/var/lib/postgresql/data/

volumes:
  db-data: